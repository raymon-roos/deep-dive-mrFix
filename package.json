{
  "name": "mr-fix",
  "version": "0.0.1",
  "description": "Mobile (web)app mockup for mrFix",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tails": "./node_modules/.bin/tailwindcss -i src/css/input.css -o dist/output.css --watch",
	"build": "./node_modules/.bin/tailwindcss -i src/css/input.css -o dist/output.css && npx workbox generateSW workbox-config.js"
  },
  "keywords": [
    "prototype",
    "mobile",
    "web",
    "app"
  ],
  "author": "",
  "license": "GPL-2.0-only",
  "devDependencies": {
    "tailwindcss": "^3.2.4",
    "workbox-cli": "^6.5.4"
  }
}
